{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:packages": "lerna run build",
    "clean-install": "rm -rf node_modules && npx lerna clean --yes && npm i --verbose"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
